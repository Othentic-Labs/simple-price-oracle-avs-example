require("dotenv").config();
// relate between peerID and address

//

function init() {

}

async function getAddressByPeerID(peerID) {
    // call AVS Logic and get address
    throw new Error("Not implemented");
}

module.exports = {
    init,
    getAddressByPeerID,
}